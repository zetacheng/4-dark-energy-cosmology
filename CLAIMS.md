# Claim Ledger

Allowed statuses: `PROPOSED`, `SUPPORTED`, `VERIFIED`, `FAILED`, `RETIRED`, `CONDITIONAL`, `INCONCLUSIVE`.

`VERIFIED` requires a closed gate, reproducible artifacts, and reviewer acceptance. `SUPPORTED` is weaker than `VERIFIED`. Failed, retired, and inconclusive claims remain recorded so that scientific history is preserved. **No claim in this repository is `VERIFIED`: no independent reviewer record exists here yet** — this branch is the reviewer's first checkpoint.

Each claim is stated as the proposition that was tested, so that its status and its text agree. Dark-matter interpretations of the migrated gates are **not** Paper 4 claims; they are recorded as cross-repo items for Paper 1 in `MIGRATION.md`.

| Claim ID | Claim | Status | Evidence | Gate | Paper section | Last reviewed |
|---|---|---|---|---|---|---|
| P4-CL-001 | The committed microscopic action contains a distinct, healthy, long-wavelength collective *interface* variable (a conserved Volovik vacuum variable or an interface height), independent of the microscopic sigma, that could seed dark-energy-interface structure. | INCONCLUSIVE | Phase-0 catalog finds the only collective scalar is the condensate amplitude = the microscopic sigma (`m_sigma = 2 m_dyn`, `xi*Lambda ~ 2.5 < 3`); candidates 2–4 are absent (positing them enlarges the model); the induced source couples to the full `epsilon` (no Volovik self-tuning). The gate cannot close positively or negatively for a wrinkle until an interface DOF is added. `results/de-interface-wrinkle/`. | P4-DEWRINKLE-01 | Pending v6.3 import | — (pending) |
| P4-CL-002 | A driven-dissipative interface EFT robustly forms and saturates a finite-wavelength (finite-k) interface wrinkle. | SUPPORTED | Generalized Swift–Hohenberg solver: finite-k band, `s_max=+0.0425`, saturation `A~0.238` confirmed by three independent methods (ETD1, IMEX-spectral, 2D). Migrated, not yet independently reviewed. `results/driven-interface-wrinkle/`. | P4-DRIVEN-01 | Pending v6.3 import | — (pending) |
| P4-CL-003 | The driven interface wrinkle is a positive-energy, pressureless, matter-like (cold-dark-matter-like) structure derivable from the committed action. | FAILED | The static pattern's equation of state is `w ~ -1/3..-1/2` (DE-like/wall-like, not pressureless); the cold-DM-like region of the 15-point scan is empty (0/15); long-wave `k_star/Lambda<=0.1` needs fine-tuning; and nothing is derived from the committed action (`MICROSCOPIC / EFT-ONLY / FAILED`). `results/driven-interface-wrinkle/`. | P4-DRIVEN-01 | Pending v6.3 import | — (pending) |
| P4-CL-004 | As an explicit 5D model enlargement, a healthy finite-tension advancing Sea→lattice wall exists, with a `w~-1` 4D source, a finite-critical-curvature reverse transition, and a regular, geodesically-complete black-hole core with a Schwarzschild exterior. | SUPPORTED | 9 of 10 sub-gates are constructible as an EFT: `sigma_A=0.9428`, `Z_h=0.943>0`, zero mode with no negatives, `w_eff~-0.997`, `P_leak/P_in~1e-12`, `I_c~0.128`, `E_unwind>0` protection, finite `R,R_mn^2,K` core, complete geodesics. Migrated, not independently reviewed. `results/sea-interface-phase-conversion/`. | P4-SEA-01 | Pending v6.3 import | — (pending) |
| P4-CL-005 | The minimal 5D action derives the map from Sea→lattice wall motion to four-dimensional cosmic expansion `a(t)` (expansion-as-cell-creation). | INCONCLUSIVE | The tenth sub-gate and binding gap: the cell-creation *rate* is definable, but the 3-space-dilation vs time-slice-creation identification is not fixed without an extra tiling/induced-metric assumption — the map is not yet defined. `results/sea-interface-phase-conversion/`. | P4-SEA-01 | Pending v6.3 import | — (pending) |
| P4-CL-006 | A driven wrinkle binds a genuinely new dark excitation through a coupling to a committed microscopic mode that is derived in-framework. | INCONCLUSIVE | The spectral binding mechanism is sound (scalar & fermion, two methods each; threshold `U0_crit*R^2~2.8-3.1`; delocalizes as `A_h->0`), and the bound mode is matter-like (`w_X~0.03`), but no coupling between the driven wrinkle and any committed mode is derived; under a condensate identification it localizes an *existing* fermion, and a new bound state is EFT-only. `results/wrinkle-bound-excitation/`. | P4-WRINKLE-EXC-01 | Pending v6.3 import | — (pending) |
| P4-CL-007 | The transmutation/monopole chain terminates: `S_mono = 640 * kappa_U = -85/(9 pi^2) = -0.95` falls outside the pre-registered window `[140, 550]` (required `+0.144`, and of opposite sign). | SUPPORTED | Paper 4's own arithmetic on the inherited input `kappa_U = -17/(1152 pi^2)`. **Cross-repo dependency:** the input `kappa_U` is `P5-CL-003` in `zetacheng/5-topological-sector` (`VERIFIED` there), not an artifact of this repository. Recorded `SUPPORTED` — the arithmetic is Paper 4's; the input is external and has no reviewer record here. | P4-MONOPOLE-01 | Pending v6.3 import | — (pending) |
