# Paper

Current Paper 4 version: **v6.3**.

The PI-supplied source `paper4_dark_energy_v6_3.tex` is **present** in this
directory. It was verified against the branch's own record on import (not
edited): it references the same terminated-chain result the gates record —
`kappa_U = -0.00149` (exact `-17/(1152 pi^2)`), `S_mono = 640 kappa_U = -0.95`
outside the pre-registered window `[140, 550]`, "the chain terminates by its
pre-registered criterion" — and is a self-contained LaTeX document
(`\end{document}` present, braces balanced). Bundled figures and a separate
bibliography file were not supplied; the paper carries an inline
`thebibliography` block, and `paper/figures/` remains empty.

Paper text may be updated only after the relevant gate has reviewer acceptance
and the Principal Investigator authorizes the change.
