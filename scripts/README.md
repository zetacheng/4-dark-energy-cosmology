# Scripts

Place symbolic, numerical, and result-processing implementations here. Each script must document its inputs, outputs, conventions, regulator, cutoff, normalization, seeds, and operating point where applicable.

Production scripts require a committed derivation note, tests, and regression anchors.
