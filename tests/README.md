# Tests

This directory contains repository-structure checks and, after historical import, scientific unit and regression tests. A passing test suite demonstrates implementation consistency; it does not by itself validate a scientific claim.
